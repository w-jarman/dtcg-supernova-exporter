import { DesignComponent } from "../../model/components/SDKDesignComponent";
import { DesignComponentGroup, DesignComponentGroupRemoteModel } from "../../model/groups/SDKDesignComponentGroup";
export declare class DesignComponentGroupResolver {
    designComponents: Array<DesignComponent>;
    constructor(designComponents: Array<DesignComponent>);
    resolveGroupData(data: Array<DesignComponentGroupRemoteModel>): Array<DesignComponentGroup>;
    private assignSourceDataToGroups;
    private recomputePaths;
    private recomputePathsFromRoot;
    private reorderGroupsByRoots;
    private traverseSortGroup;
    private recomputeParents;
    private recomputeParentsFromRoot;
}
