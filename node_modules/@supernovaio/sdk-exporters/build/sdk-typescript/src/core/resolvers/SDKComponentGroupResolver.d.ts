import { Component } from "../../model/components/SDKComponent";
import { ComponentGroup, ComponentGroupRemoteModel } from "../../model/groups/SDKComponentGroup";
export declare class ComponentGroupResolver {
    components: Array<Component>;
    constructor(components: Array<Component>);
    resolveGroupData(data: Array<ComponentGroupRemoteModel>): Array<ComponentGroup>;
    private recomputePaths;
    private recomputePathsFromRoot;
    private reorderGroupsByRoots;
    private traverseSortGroup;
    private recomputeParents;
    private recomputeParentsFromRoot;
}
