import { TokenGroup, TokenGroupRemoteModel } from "../../model/groups/SDKTokenGroup";
export declare class TokenGroupResolver {
    resolveGroupData(data: Array<TokenGroupRemoteModel>): Array<TokenGroup>;
    private recomputePaths;
    private recomputePathsFromRoot;
    private reorderGroupsByRoots;
    private traverseSortGroup;
    private recomputeParents;
    private recomputeParentsFromRoot;
}
