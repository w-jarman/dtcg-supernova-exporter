import { DTOMCPStreamUpdateInput } from "@supernova-studio/client";
import { MCPStream } from "../../model/mcp/SDKMCPStream";
import { DataCore } from "../data/SDKDataCore";
export declare class AreaMCP {
    private dataCore;
    constructor(dataCore: DataCore);
    /**
     * Retrieves an MCP stream by ID. Users can only access MCP streams associated with themselves.
     * @param streamId - The ID of the MCP stream to retrieve.
     * @returns The MCPStream object.
     */
    getMCPStream(streamId: string): Promise<MCPStream>;
    /**
     * Retrieves the user's MCP stream. If no MCP stream exists for the user, an empty MCP stream is created automatically.
     * @returns The user's MCPStream object.
     */
    getUserMCPStream(): Promise<MCPStream>;
    /**
     * Updates the user's MCP stream with the provided data.
     * @param data - The update input containing optional projectFeatureId and projectDocumentId.
     * @returns The updated MCPStream object.
     */
    updateUserMCPStream(data: DTOMCPStreamUpdateInput): Promise<MCPStream>;
}
