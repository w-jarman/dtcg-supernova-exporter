import { CodeComponent } from "../../model/code-components/SDKCodeComponent";
import { DataCore } from "../data/SDKDataCore";
import { RemoteVersionIdentifier } from "./SDKRemoteIdentifiers";
export declare class AreaCodeComponents {
    /** Internal: Engine */
    private dataCore;
    constructor(dataCore: DataCore);
    /** Fetches all remote code components in the version
     * @param from - Remote version to fetch from
     * @returns All components in the specified version
     */
    getCodeComponents(from: RemoteVersionIdentifier): Promise<Array<CodeComponent>>;
}
