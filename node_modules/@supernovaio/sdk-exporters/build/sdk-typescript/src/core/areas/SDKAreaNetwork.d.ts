import { DataCore } from "../data/SDKDataCore";
export declare class AreaNetwork {
    /** Internal: Engine */
    private dataCore;
    constructor(dataCore: DataCore);
    /** Fires network request using either node-fetch or native fetch if available inside the environment sdk runs in */
    fetch(url: RequestInfo, init?: RequestInit): Promise<Response>;
}
