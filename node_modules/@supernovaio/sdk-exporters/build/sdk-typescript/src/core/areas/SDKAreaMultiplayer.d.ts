import { DataCore } from "../data/SDKDataCore";
export declare class AreaMultiplayer {
    /** Internal: Engine */
    private dataCore;
    constructor(dataCore: DataCore);
    /** Return multiplayer access token for specific room. */
    getMultiplayerToken(room: string | undefined): Promise<{
        token: string;
    }>;
}
