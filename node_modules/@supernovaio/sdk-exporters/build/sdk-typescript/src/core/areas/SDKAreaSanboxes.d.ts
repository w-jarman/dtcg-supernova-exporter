import { SandboxTemplate } from "../../model/sandboxes/SDKSandboxTemplate";
import type { DataCore } from "../data/SDKDataCore";
export declare class AreaSandboxes {
    private dataCore;
    constructor(dataCore: DataCore);
    getSandboxTemplateList({ workspaceId, designSystemId, }: {
        workspaceId: string;
        designSystemId?: string;
    }): Promise<SandboxTemplate[]>;
    sandboxKeepAlive(workspaceId: string, projectId: string, featureId: string, sandboxId: string): Promise<{
        id: string;
        url: string;
        expiresAt: string;
        featureId: string;
        currentIterationId: string;
        error?: {
            stage: "Build" | "PackageInstall" | "Unknown" | "HealthCheck";
            errorDescription: string;
        } | undefined;
        parentMessageId?: string | undefined;
    }>;
    provisionSandbox(workspaceId: string, projectId: string, featureId: string, parentMessageId?: string): Promise<{
        ok: boolean;
    }>;
}
