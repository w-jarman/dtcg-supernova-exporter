import { Asset } from "../../../model/assets/SDKAsset";
import { RenderedAsset } from "../../../model/assets/SDKRenderedAsset";
import { AssetFormat } from "../../../model/enums/SDKAssetFormat";
import { AssetScale } from "../../../model/enums/SDKAssetScale";
import { AssetGroup } from "../../../model/groups/SDKAssetGroup";
import { DataBridge } from "../SDKDataBridge";
export type RenderedAssetRequestSettings = Array<{
    prefix: string;
    suffix: string;
    scale: AssetScale;
    format: AssetFormat;
}>;
export declare class DataCoreRenderRequests {
    private bridge;
    constructor(bridge: DataBridge);
    renderAssetsForConfiguration(designSystemId: string, versionId: string, settings: RenderedAssetRequestSettings, assets: Array<Asset>, groups: Array<AssetGroup>): Promise<Array<RenderedAsset>>;
    private assetPath;
}
