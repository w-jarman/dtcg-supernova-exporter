import { DTOriginalTokenType } from "../../../model/enums/SDKTokenType";
import { Logger } from "../../../model/support/SDKLogger";
export type DTParsedNode = {
    rootKey: string;
    name: string;
    path: Array<string>;
    type: DTOriginalTokenType;
    value: any;
    description: string | null;
};
export type DTParsedTokenSet = {
    name: string;
    id: string;
    contains: Array<DTParsedNode>;
};
export type DTParsedTheme = {
    name: string;
    id: string;
    selectedTokenSets: Array<DTParsedThemeSetPriorityPair>;
};
export type DTParsedThemeSetPriorityPair = {
    set: DTParsedTokenSet;
    priority: DTParsedThemeSetPriority;
};
export declare enum DTParsedThemeSetPriority {
    source = "source",
    enabled = "enabled",
    disabled = "disabled"
}
/** Design Tokens Plugin Manipulation Tool */
export declare class DTJSONParser {
    themeBuffer: Array<DTParsedTheme>;
    logger: Logger;
    constructor(logger: Logger);
    processPluginDataRepresentation(definition: object): {
        nodes: Array<DTParsedNode>;
        themes: Array<DTParsedTheme>;
        sets: Array<DTParsedTokenSet>;
    };
    private processThemes;
    private processSets;
    private parseNode;
    private modifyEntityForParse;
}
