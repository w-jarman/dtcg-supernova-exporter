import { DTProcessedTokenNode } from "../SDKDTJSONConverter";
export declare class DTProcessedTokenNodeCollection {
    readonly index: Map<string, DTProcessedTokenNode>;
    readonly added: Set<string>;
    readonly deleted: Map<string, DTProcessedTokenNode>;
    readonly dirty: Set<string>;
    readonly list: Array<DTProcessedTokenNode>;
    constructor(source: Array<DTProcessedTokenNode> | Map<string, DTProcessedTokenNode>);
    markDeleted(element: DTProcessedTokenNode): void;
    removeFromCollection(persistentId: string): void;
    markDirty(node: DTProcessedTokenNode): void;
    add(node: DTProcessedTokenNode): void;
}
