import { DocsSectionType } from "../../enums/SDKDocsSectionType";
import { DocsColorRef } from "../blocks/support/SDKDocsColorRef";
import { DocsSectionItemModel } from "./SDKDocsSectionItem";
export type DocsSectionModel = {
    id: string;
    type: DocsSectionType;
    appearance?: {
        expandToEdges: boolean;
        contentExpandToEdges: boolean;
        backgroundColor?: DocsColorRef;
        foregroundColor?: DocsColorRef;
        padding?: {
            top?: number;
            bottom?: number;
            left?: number;
            right?: number;
        };
    };
    items: Array<DocsSectionItemModel>;
};
export declare class DocsSection {
    /** Unique identifier of section, uuidv4() on create * */
    id: string;
    /** Section type */
    type: DocsSectionType;
    /** Global appearance properties for the section like background color, expanding to edges, etc. */
    appearance: {
        /** Section background is expanded to fill available area, including side margins */
        expandToEdges: boolean;
        /** Content of the section is expanded to fill available area, including side margins */
        contentExpandToEdges: boolean;
        /** Background color of the section */
        backgroundColor: DocsColorRef | null;
        /** Foreground color of the section */
        foregroundColor: DocsColorRef | null;
        /** Content offset from the sides */
        padding: {
            top: number;
            bottom: number;
            left: number;
            right: number;
        };
    };
    /** Section items contained within this section. At least one is always present */
    items: Array<DocsSectionItemModel>;
    constructor(model: DocsSectionModel);
}
