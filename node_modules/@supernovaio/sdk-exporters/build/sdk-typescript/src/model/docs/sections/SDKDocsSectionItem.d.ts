import { DocsSectionColumn, DocsSectionColumnModel } from "./SDKDocsSectionColumn";
export type DocsSectionItemModel = {
    id: string;
    title: string;
    columns: Array<DocsSectionColumnModel>;
};
export declare class DocsSectionItem {
    /** Unique identifier of section item, uuidv4() on create * */
    id: string;
    /** User-provided item title */
    title: string;
    /** Section item columns contained within this section item. At least one is always present */
    columns: Array<DocsSectionColumn>;
    constructor(model: DocsSectionItemModel);
}
