import { SizeTokenValue } from "../../../tokens/SDKTokenValue";
/** A size data container */
export type DocsSizeRef = {
    /** Width value - either token reference or raw value */
    width?: SizeTokenValue;
    /** Height value - either token reference or raw value */
    height?: SizeTokenValue;
};
