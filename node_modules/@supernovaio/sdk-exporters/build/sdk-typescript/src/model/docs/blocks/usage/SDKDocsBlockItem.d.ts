import { PageBlockItemV2 } from "@supernova-studio/model";
import { DocsLinkRef } from "../support/SDKDocsLinkRef";
export type DocsBlockItemModel = PageBlockItemV2;
export declare class DocsBlockItem {
    /** Unique identifier of the block item */
    id: string;
    /** Dynamic key coming from property.id block definition */
    props: {
        [key: string]: object;
    };
    /** Link where the item points to, if any. Must be enabled in behaviors */
    linksTo: DocsLinkRef | undefined;
    constructor(model: DocsBlockItemModel);
    /** Constructs representation that can be used to write full object to remote */
    toRemote(): DocsBlockItemModel;
}
