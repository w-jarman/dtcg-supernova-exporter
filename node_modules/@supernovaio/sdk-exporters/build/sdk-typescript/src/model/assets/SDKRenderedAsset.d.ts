import { AssetFormat } from "../enums/SDKAssetFormat";
import { AssetScale } from "../enums/SDKAssetScale";
import { AssetGroup } from "../groups/SDKAssetGroup";
import { Asset } from "./SDKAsset";
export type RenderedAssetModel = {
    assetId: string;
    fileName: string;
    sourceUrl: string;
    settings: {
        prefix: string;
        suffix: string;
        scale: AssetScale;
        format: AssetFormat;
    };
    originalName: string;
};
export declare class RenderedAsset {
    assetId: string;
    fileName: string;
    originalName: string;
    sourceUrl: string;
    asset: Asset;
    group: AssetGroup;
    scale: AssetScale;
    format: AssetFormat;
    previouslyDuplicatedNames: number;
    constructor(model: RenderedAssetModel, asset: Asset, group: AssetGroup, duplicates: number);
}
