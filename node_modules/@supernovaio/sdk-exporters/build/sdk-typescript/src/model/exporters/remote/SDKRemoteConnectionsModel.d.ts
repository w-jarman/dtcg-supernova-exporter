import { ConnectionProviders } from "../../enums/SDKConnectionProviders";
export type ConnectionRemoteModel = {
    id: string;
    ownerId: string;
    customUrl: string;
    name: string;
    authType: "PAT";
    service: ConnectionProviders;
};
