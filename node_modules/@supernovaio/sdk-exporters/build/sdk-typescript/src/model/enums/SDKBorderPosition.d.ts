export declare enum BorderPosition {
    inside = "Inside",
    center = "Center",
    outside = "Outside"
}
export declare const ALL_BORDER_POSITIONS: readonly [BorderPosition.inside, BorderPosition.center, BorderPosition.outside];
