export declare enum WorkspaceNPMRegistryAuthType {
    /** The workspace is configured to be able to access registry with Basic auth */
    basic = "Basic",
    /** The workspace is configured to be able to access registry using Bearer token */
    bearer = "Bearer"
}
