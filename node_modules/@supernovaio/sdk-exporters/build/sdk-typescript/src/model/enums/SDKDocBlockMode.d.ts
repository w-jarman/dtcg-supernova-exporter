export declare enum DocsBlockMode {
    /** Block has static items, and only one of it */
    singleItem = 0,
    /** Block has static items, and multiple of them */
    multipleItems = 1,
    /** Block has dynamic data drawn from Supernovva */
    data = 2
}
