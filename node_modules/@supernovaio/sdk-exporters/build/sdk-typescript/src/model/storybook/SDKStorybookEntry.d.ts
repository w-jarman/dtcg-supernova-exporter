import { DTOStorybookEntry } from "@supernova-studio/client";
export type RemoteStorybookEntry = Omit<DTOStorybookEntry, "createdAt" | "updatedAt"> & {
    createdAt: string;
    updatedAt: string;
};
export declare class StorybookEntry {
    id: string;
    url: string;
    createdAt: Date;
    updatedAt: Date;
    sourceId: string;
    designSystemId: string;
    storyId: string;
    origin: {
        type: "story" | "docs";
        name: string;
        id: string;
        title: string;
    };
    aliases: string[] | null;
    isDeleted: boolean | null;
    constructor(entry: RemoteStorybookEntry);
}
