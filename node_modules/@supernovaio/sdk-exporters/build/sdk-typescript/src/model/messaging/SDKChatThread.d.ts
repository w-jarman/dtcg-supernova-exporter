import { DTOForgeChatThread } from "@supernova-studio/client";
import { ChatThreadMessage } from "./SDKChatThreadMessage";
export declare class ChatThread {
    id: string;
    userId: string;
    workspaceId: string;
    title: string;
    createdAt: Date;
    updatedAt: Date;
    messages?: Array<ChatThreadMessage>;
    constructor(model: DTOForgeChatThread, messages?: Array<ChatThreadMessage>);
    copyWithMessages(messages: Array<ChatThreadMessage>): ChatThread;
    toRemote(): DTOForgeChatThread;
}
